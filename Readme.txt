git clone https://github.com/TIOPAULOVEIODOSACO/desenv-sistemas

git config --global user.name "agner maurilio dos santos junior"

git config --global user.email "agner.junor@escola.pr.gov.br"

git add .

git commit -m "Sua mensagem de commit aqui"

git push
